// Version.swift
// Centralized version management for HPDebugConfigSDK

struct HPDebugKitVersion {
    static let major = 1
    static let minor = 0
    static let patch = 0
    
    static let version = "\(major).\(minor).\(patch)"
    static let buildNumber = "1"
    
    static let fullVersion = "\(version) (\(buildNumber))"
}
